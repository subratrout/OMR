<%= simple_form_for(@pin) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :description %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
